<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.captcha_solver_flutter">

    <application
        android:label="captcha_solver_flutter"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">  <!-- إذا تحتاج HTTP غير مشفر -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- احذف أي service/receiver خاصّ بالإصدار القديم -->
    </application>
</manifest>
