package com.example.captcha_solver_flutter  // عدّل حسب package name في build.gradle

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // لا حاجة لأي overrides لأن v2 embedding يتولّى كل شيء تلقائياً
}
